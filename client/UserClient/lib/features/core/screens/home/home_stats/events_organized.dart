import 'package:flutter/material.dart';

class EventsOrganized extends StatelessWidget {
  const EventsOrganized({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Coming Soon")),
    );
  }
}
