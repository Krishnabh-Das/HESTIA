import 'package:flutter/material.dart';

class CrimeIncidents extends StatelessWidget {
  const CrimeIncidents({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Coming Soon............"),
    );
  }
}
